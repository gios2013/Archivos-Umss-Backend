server:
  port: 8083

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/prod_aub_db
    username: test1
    password: can
    hikari:
      poolName: Hikari
      auto-commit: false
      maximum-pool-size: 8

  flyway:
    locations: classpath:db/migration,classpath:prod/db/migration