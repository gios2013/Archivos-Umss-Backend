create TABLE nationality (
                           id SMALLSERIAL PRIMARY KEY,
                           name varchar (55) not null,
                           demonym character varying(55),
                           code character varying(55) NOT NULL

);